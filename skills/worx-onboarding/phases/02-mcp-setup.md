# Phase 2: MCP Server Configuration

Delegate to the worx-mcp-config skill.

## Execute

Invoke `/worx-mcp-config` skill logic:

1. Check current MCP servers
2. Present status table
3. Ask user which to configure
4. Configure using `claude mcp add` CLI
5. Report results

After completion, proceed to Phase 3.
